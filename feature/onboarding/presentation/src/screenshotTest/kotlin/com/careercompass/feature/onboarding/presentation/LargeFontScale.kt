package com.careercompass.feature.onboarding.presentation

/**
 * 큰 글꼴 골든이 쓰는 배율 — 근거와 화면 선정 기준은 `docs/testing/screenshot.md`.
 *
 * 2.0 은 Android 14 가 올린 글꼴 크기 설정 상한(200%)이다. screenshotTest 소스셋은 모듈 밖으로
 * 나가지 않아 모듈마다 같은 값을 둔다.
 */
internal const val LARGE_FONT_SCALE: Float = 2.0f
