# CareerCompass 사용자 매뉴얼

앱을 처음 여는 사람이 가입부터 공고 상세까지 혼자 갈 수 있게 쓴 안내다.

**이 매뉴얼은 앱에 실제로 있는 것만 적는다.** 화면 문구는 저장소의 문자열 리소스와 Composable 에서 그대로 옮겼고, 아직 만들지 않은 화면과 버튼은 여기에도 없다.

- 기준: `develop` `461af42`
- 캡처는 아직 없다. 실서버가 붙은 뒤에 찍어야 화면에 실제 데이터가 담기므로, 지금은 들어갈 자리만 `> 캡처 자리:` 줄로 표시해 두었다.

## 차례

| 부 | 문서 | 무엇을 다루는가 |
| --- | --- | --- |
| 1부 | [가입과 온보딩](01-signup-and-onboarding.md) | 소셜 로그인, 지문 로그인, 온보딩 4단계, 완료 화면 |
| 2부 | [게시판](02-boards.md) | URL 등록, 구조 분석 결과, 이름·유형·수집 주기, 내 게시판 목록 |
| 3부 | [피드와 적합도](03-feed-and-suitability.md) | 검색·필터·정렬·북마크, 적합도 4축, 공고 상세와 원문 보기 |

읽는 순서는 위에서 아래다. 1부를 마치면 계정이 생기고, 2부를 마치면 공고가 들어오기 시작하고, 3부는 들어온 공고를 고르는 법이다. **게시판을 하나도 등록하지 않으면 피드에 아무것도 쌓이지 않는다.** 앱이 스스로 공고를 찾아 오지 않는다는 것이 처음 쓰는 사람이 가장 많이 막히는 자리라, 2부를 건너뛰지 않기를 권한다.

## 아직 쓰지 않은 부

지원서(4부), 마이 탭(5부), 알림(6부)은 그 화면이 앱에 들어온 뒤에 쓴다. 지금 하단 탭의 「분석」·「지원서」와 피드 헤더의 알림 버튼을 누르면 준비 중이라는 자리표시자가 나온다. 매뉴얼 전체의 범위는 이슈 #268 에 있고, 이 세 부는 #289 로 갈라 먼저 썼다.

## 이 매뉴얼이 값을 적지 않는 것

- 적합도 축마다 붙는 비율은 서버가 공고마다 내려 준다. 앱이 들고 있는 고정값이 아니라서 여기에 숫자를 적지 않는다.
- 수집이 몇 번 실패하면 자동으로 멈추는지 같은 판정도 서버가 한다. 앱은 그 결과를 배지와 안내로 보여 줄 뿐이라 그 값도 적지 않는다.

## 문구를 어디서 확인하는가

매뉴얼이 「」 로 감싼 문장은 앱 화면에 그대로 나오는 말이다. 원본은 아래 파일들에 있다.

| 영역 | 파일 |
| --- | --- |
| 로그인·지문·온보딩 | [`feature/onboarding/presentation/src/main/res/values/`](../../feature/onboarding/presentation/src/main/res/values) |
| 피드·공고·게시판 | [`feature/feed/presentation/src/main/res/values/`](../../feature/feed/presentation/src/main/res/values) |
| 실패·상태 화면 공통 | [`core/ui/src/main/res/values/`](../../core/ui/src/main/res/values) |

화면별 엣지 상태(오프라인·로딩·빈 결과·점검·세션 만료)의 확정표는 [`docs/spec/edge-states.md`](../spec/edge-states.md), 서버 에러 코드의 문구 매핑은 [`docs/spec/error-copy.md`](../spec/error-copy.md) 에 있다.
